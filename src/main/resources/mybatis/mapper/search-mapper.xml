<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.multi.semiproject.search.model.dao.SearchMapper">

    <select id="findByTitle" resultType="com.multi.semiproject.search.model.dto.SearchResultDTO">
        SELECT no, district, title, description, address, phone
        FROM travel
        WHERE title LIKE CONCAT('%', #{query}, '%')
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByTitle" resultType="int">
        SELECT COUNT(*)
        FROM travel
        WHERE title LIKE CONCAT('%', #{query}, '%')
    </select>

</mapper>
